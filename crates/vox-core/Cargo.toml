[package]
name = "vox-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Platform-independent core for Vox: hotkey chords, session state machine, audio segmentation, config."

[dependencies]
serde.workspace = true
toml.workspace = true
thiserror.workspace = true
